// Registra los matchers de jest-dom (toBeInTheDocument, etc.) en el `expect` de Vitest y
// habilita la limpieza automática del DOM entre pruebas (vía globals de Vitest).
import "@testing-library/jest-dom/vitest";
